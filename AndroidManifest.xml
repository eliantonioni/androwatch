<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aeliseev.androwatch"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="15" android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>

    <application android:icon="@drawable/app_icon" android:label="@string/app_name">

        <!-- Broadcast Receiver that will process AppWidget updates -->
        <receiver android:name="com.aeliseev.androwatch.WordWidget" android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/widget_word" />
        </receiver>

        <receiver android:name="com.aeliseev.androwatch.Alarm">
            <intent-filter>
                <action android:name="com.aeliseev.androwatch.Alarm1"></action>
                <action android:name="com.aeliseev.androwatch.Alarm2"></action>
                <action android:name="com.aeliseev.androwatch.Alarm3"></action>

                <action android:name="com.aeliseev.androwatch.Alarm1.Stop"></action>
                <action android:name="com.aeliseev.androwatch.Alarm2.Stop"></action>
                <action android:name="com.aeliseev.androwatch.Alarm3.Stop"></action>
            </intent-filter>
        </receiver>

    </application>

</manifest>
